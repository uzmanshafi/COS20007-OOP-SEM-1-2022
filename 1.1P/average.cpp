#include <iostream>
using namespace avg;

double Avg(int num[], int n)
{
    int avg = 0;

    for(int i=0;i<n;i++)
    sum += num[i]

    return (double)sum/n;
}

int main()
{
    int array[] = (10,15,20,25,30,35,40,45,50);
    int n = sizeof(array)/sizeof(array[0]);

    cout << Avg(array , n) << endl;
    return 0;
}